{
  "private": true,
  "workspaces": [
    "frontend",
    "server"
  ],
  "devDependencies": {
    "husky": "^9.1.7"
  },
  "scripts": {
    "prepare": "husky || true",
    "build": "cd frontend && npm run build && cp -r dist ../public || xcopy /E /I dist ..\\public"
  },
  "dependencies": {
    "pg-dump-restore-nodejs": "^0.0.10",
    "@supabase/supabase-js": "^2.97.0"
  }
}
