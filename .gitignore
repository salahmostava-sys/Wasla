# Environment variables
.env
.env.local
.env.*.local
**/credentials/
**/vision-key.json

# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr

# Build outputs & generated artifacts (do not commit — keeps language stats accurate)
dist/
frontend/dist/
build/
build
frontend/coverage/
coverage/
*.local

# Mockup sandbox (Replit artifact — not part of the app)
artifacts/

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Vercel local / linked output (keep vercel.json in repo if needed)
.vercel/
.vercel

# AI tool metadata / prompts
.lovable
.claude
.cursor
.ai
prompt.md
*.prompt
/src/

# IDE / local tool configs
.sonarlint/
.sixth/

maestro/

# Python cache
__pycache__/
*.pyc
.circleci/

# Replit agent — skills, tasks, session data (not part of the app)
.local/

# Replit attached paste files (auto-generated, not part of the codebase)
attached_assets/

# Replit environment config (not needed on GitHub)
.replit

# Auto-generated reports (not part of the codebase)
REPO_CLEANUP_REPORT.md

# Lockfiles from other package managers (project uses npm)
frontend/bun.lock
bun.lock
bun.lockb
yarn.lock
# Environment variables
.env
.env.local
.env.*.local
**/credentials/
**/vision-key.json

# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr

# Build outputs & generated artifacts (do not commit — keeps language stats accurate)
dist/
frontend/dist/
build/
build
frontend/coverage/
coverage/
*.local

# Mockup sandbox (Replit artifact — not part of the app)
artifacts/

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Vercel local / linked output (keep vercel.json in repo if needed)
.vercel/
.vercel

# AI tool metadata / prompts
.lovable
.claude
.cursor
.ai
prompt.md
*.prompt
/src/

# IDE / local tool configs
.sonarlint/
.sixth/

maestro/

# Python cache
__pycache__/
*.pyc
.circleci/

# Replit agent — skills, tasks, session data (not part of the app)
.local/

# Replit attached paste files (auto-generated, not part of the codebase)
attached_assets/

# Replit environment config (not needed on GitHub)
.replit

# Auto-generated reports (not part of the codebase)
REPO_CLEANUP_REPORT.md

# Lockfiles from other package managers (project uses npm)
frontend/bun.lock
bun.lock
bun.lockb
yarn.lock
pnpm-lock.yaml

# Reports and Dumps
sonar_issues*.json
sonar_report.md
pg.zip
scratch/
graphify-out/
public/assets/

# Root-level public/ is generated build output (see root package.json "build"
# script: it wipes and recreates this folder from frontend/dist on every
# build/deploy). It should never be committed to git.
/public/