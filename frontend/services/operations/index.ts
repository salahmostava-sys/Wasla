/**
 * Operations Domain Services
 * Import from here for domain-organized access:
 *   import { orderService } from '@services/operations';
 *
 * Existing per-file imports still work:
 *   import { orderService } from '@services/orderService';
 */

export { orderService } from '@services/orderService';

export { shiftService } from '@services/shiftService';

export { fuelService } from '@services/fuelService';

export { violationService } from '@services/violationService';

export { performanceService } from '@services/performanceService';
