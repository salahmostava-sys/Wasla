/**
 * Finance Domain Services
 * Import from here for domain-organized access:
 *   import { salaryService } from '@services/finance';
 *
 * Existing per-file imports still work:
 *   import { salaryService } from '@services/salaryService';
 */

export { salaryService } from '@services/salaryService';

export { salaryDataService } from '@services/salaryDataService';

export { salaryDraftService } from '@services/salaryDraftService';

export { salarySchemeService } from '@services/salarySchemeService';

export { salarySlipTemplateService } from '@services/salarySlipTemplateService';

export { advanceService } from '@services/advanceService';

export { financeService } from '@services/financeService';

export { treasuryService } from '@services/treasuryService';

export { hybridRuleService } from '@services/hybridRuleService';

export { walletService } from '@services/walletService';

export { externalDeductionService } from '@services/externalDeductionService';
