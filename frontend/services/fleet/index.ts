/**
 * Fleet Domain Services
 * Import from here for domain-organized access:
 *   import { vehicleService } from '@services/fleet';
 *
 * Existing per-file imports still work:
 *   import { vehicleService } from '@services/vehicleService';
 */

export { vehicleService } from '@services/vehicleService';

export { getVehicleReport } from '@services/vehicleReportService';

export { maintenanceService } from '@services/maintenanceService';
